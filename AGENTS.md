# Project instructions

Work in this repository. The original Reforecast, NextGen_Reforecast, HYDRA and thesis workspaces remain read-only sources; never write reports, caches, Git metadata or generated outputs into them or force iCloud hydration.

The original design and task prompt under `nextgen_Software/` and the completed feasibility dossier are historical evidence and must remain unchanged. The owner subsequently authorized the public repository and, on 18 September 2026, implementation toward a completed product and manuscript. This supersedes their earlier feasibility-only stop condition. Follow `IMPLEMENTATION_PLAN.md` for current scope and file ownership.

The owner supplied an expanded roadmap on 22 September 2026. Preserve its exact copy under `docs/source_material/`; use `docs/ROADMAP_RECONCILIATION.md`, `docs/DEVELOPMENT_BACKLOG.md` and `paper/DEVELOPMENT_PLAN.md` for current assignments. Treat its historical capability statements as source claims, not public-runtime qualification. Preserve existing milestone IDs and fixture origins. Conference deadlines and upstream product claims require current primary-source verification.

The 23 September product-planning update is recorded in `docs/PRODUCT_OBJECTIVES_AND_USER_JOURNEYS.md` and `docs/UX_EVALUATION_PROTOCOL.md`. It supersedes earlier assumptions that researchers provide prepared inputs, use Python/CLI, or receive a hosted service only after the first usable product. Preserve the distinction between owner-confirmed requirements, proposed modes and implemented capabilities. Future assignments must name objective/journey IDs and acceptance evidence. This request saves planning documents only; it does not initiate implementation, deployment, data-acquisition jobs or spending. The previously verified offline CLI remains the current implementation.

The 18 September implementation phase authorized ordinary bounded local code/dependency/test work and established the offline planning foundation. The current 23 September request is documentation-only. At a subsequent implementation kickoff, develop and test the new package locally against the updated objectives, runtime/asset closure requirements and evidence-bound manuscript plan. Do not claim an unimplemented CLI, an unexecuted model, a synthetic fixture or historical receipt is a qualified public runtime. Keep unknown measurements unknown.

Keep original physical inputs, origins, parameters, forcing/time semantics, receipts and source identities intact. A routing snapshot is not a complete land checkpoint. Do not start paid cloud resources or publish messages/manuscript submissions. Native/model execution requires a complete bounded recipe and verified assets/environment; those are not yet established in this first increment.

Avoid copying inherited implementation, binaries or datasets into the public repository until exact source/ownership/redistribution scope is resolved. Preserve third-party notices. A project-wide license has not been selected. At an implementation kickoff, new core implementation may be authored independently from the documented contracts.

Use targeted tests for meaningful contract failures: clock boundaries, missing/corrupt assets, unsupported profiles, branch/commit identity and runtime restrictions. Keep test writes inside repository-local temporary directories. Update implemented-feature and evidence status accurately; preserve prior completed dossier bytes.
