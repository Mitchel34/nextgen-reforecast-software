# Project instructions

Work in this repository only. The original Reforecast, NextGen_Reforecast, HYDRA, and thesis workspaces are read-only sources; never write reports, caches, Git metadata, or generated outputs into them.

The original design and task prompt are preserved under `nextgen_Software/`. The owner subsequently authorized creation and publication of this separate public repository. That authorization supersedes the older repository-creation and publication prohibition in the preserved prompt. Its remaining scope is a read-only feasibility investigation ending with an evidence dossier and extraction recommendation.

Do not treat proposed commands as installed software, historic reports as new test results, source locators as available data, or routing snapshots as complete land checkpoints. Distinguish inspected source, tests defined, reported historic checks, inspected historic receipts, and checks newly performed.

Do not select a public license, copy upstream/private research implementation or datasets, run scientific models, rebuild native components, install dependencies, or create cloud resources as part of this feasibility stage. Future implementation must be scoped explicitly by the owner.
